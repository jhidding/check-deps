# ~/~ begin <<README.md#example/first/dependencies.toml>>[init]
[awk]
require = ">=6"
get_version = "awk --version"
pattern = "GNU Awk (.*), API .*"
suggestion_text = "This should be available from your package manager."
# ~/~ end
